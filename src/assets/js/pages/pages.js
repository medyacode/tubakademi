export default function pages() {

    var Site = global.Site || {};

    (function(app) {
        'use strict';
    
        app.pages = {};
    
        var _this = app.pages;
    
        _this.init = function() {
    
          // Pages Init

        };
    
    })(Site);

    global.Site = $.extend(global.Site, Site);

}

